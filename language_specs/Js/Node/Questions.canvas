{
	"nodes":[
		{"id":"9f254669a610eb21","type":"text","text":"**1. Asynchronous Patterns:** \"In a Node.js environment, how do you handle a scenario where a service must process thousands of concurrent database writes without exhausting the connection pool or blocking the Event Loop? Specifically, how would you use TypeScript to ensure type safety across these async operations?\"\n\n\n> \n> \n> \n> \"To handle thousands of concurrent writes, I wouldn't just fire off 1,000 promises at once because I'll hit the PostgreSQL connection limit. I’d use a **Message Queue** (like RabbitMQ) to buffer the load. For the code itself, I’d implement **batching** or use a **concurrency limit** to keep the Event Loop healthy.\n> \n> Regarding TypeScript, I ensure type safety by using **Generics** in my service layers and validating the data coming out of the async calls with **Interfaces** or **Zod**. This ensures that even though the data is moving asynchronously, the rest of the application remains predictable and type-safe.\"\n> \n> \n> \n> \n> \n\n\n\n \n\n---\n\n### 1. The Event Loop & Performance\n\n**Question:** _\"We have an endpoint that performs some heavy data transformation on a large JSON payload before saving it. We’ve noticed that when this endpoint is hit, the rest of the API becomes unresponsive for a few seconds. Why is this happening, and how would you fix it?\"_\n\n**Your Answer:**\n\n> \"This is happening because the CPU-bound transformation blocks the Main Thread. To fix this, I would offload the work. For immediate processing, I’d use a **pre-warmed Worker Pool** (to avoid the high startup cost of spawning new threads). Or, for better scalability, I'd move the task to a **background worker via a Task Queue**, allowing the API to stay 100% focused on routing and I/O.\"\n\n---\n\n### 2. Database Concurrency & Race Conditions\n\n**Question:** _\"Imagine two users try to 'claim' the last available discount code at the exact same millisecond. How do you ensure in PostgreSQL that only one user gets it and we don't end up with a negative balance?\"_\n\n**Your Answer:**\n\n> \"I would handle this using **Database Transactions** with appropriate isolation levels or **Row-Level Locking**. Specifically, I’d use a `SELECT ... FOR UPDATE` query. This locks the specific row until the transaction is committed, forcing the second request to wait. This ensures data integrity and prevents 'Race Conditions.' I’d also make sure the database has a `CHECK` constraint as a final safety layer to prevent the 'count' from ever dropping below zero.\"\n\n---\n\n### 3. Messaging Queues & Reliability (The \"Must-Have\")\n\n**Question:** _\"In our architecture, Service A sends a message to Service B via RabbitMQ. If Service B is down for maintenance, what happens to that message? How do you ensure we don't lose data?\"_\n\n**Your Answer:**\n\n> \"We ensure reliability through **Message Durability** and **Acknowledgments (ACKs)**. I would configure the queue and the messages to be 'Persistent' so they are saved to disk. When Service B is down, the message stays in the queue. Once Service B comes back online, it will consume the message and send an 'ACK' back to RabbitMQ. If Service B crashes _during_ processing, the ACK is never sent, and RabbitMQ will re-queue the message so it can be tried again. This ensures 'At-Least-Once' delivery.\"\n\n---\n\n### 4. Memory Management\n\n**Question:** _\"If you have to fetch 500,000 rows from PostgreSQL and transform them into a CSV for a user to download, how would you implement this to avoid an 'Out of Memory' (OOM) error?\"_\n\n**Your Answer:**\n\n> \"I would never load 500,000 rows into an array in memory. Instead, I would use **Database Cursors** and **Node.js Streams**. I’d stream the rows from Postgres and pipe them through a transformation stream (to convert them to CSV format) and then directly into the HTTP response object. This keeps the memory usage constant—around 20-30MB—regardless of whether I'm processing 500 rows or 5 million.\"\n\n---\n\n### 5. System Design & Ownership\n\n**Question:** _\"We are moving some services to Kubernetes and GCP. How do you handle 'Graceful Shutdown' in your Node.js applications when a pod is being terminated?\"_\n\n**Your Answer:**\n\n> \"When K8s sends a `SIGTERM` signal, the app shouldn't just die instantly. I listen for that signal in the code, stop the server from accepting new connections, and finish any 'in-flight' requests or database transactions. I also make sure to close database pools and MQ connections properly. Only after all pending work is done do I call `process.exit(0)`. This prevents data corruption and 502 errors during deployments.\"","x":-4560,"y":-5600,"width":724,"height":2400},
		{"id":"ffa89225a8b8bbaf","x":-7723,"y":-2400,"width":760,"height":490,"type":"text","text":"### 1. The Event Loop & Performance\n\n**Question:** _\"We have an endpoint that performs some heavy data transformation on a large JSON payload before saving it. We’ve noticed that when this endpoint is hit, the rest of the API becomes unresponsive for a few seconds. Why is this happening, and how would you fix it?\"_\n\n**Your Answer:**\n\n> \"This is happening because the CPU-bound transformation blocks the Main Thread. To fix this, I would offload the work. For immediate processing, I’d use a **pre-warmed Worker Pool** (to avoid the high startup cost of spawning new threads). Or, for better scalability, I'd move the task to a **background worker via a Task Queue**, allowing the API to stay 100% focused on routing and I/O.\"\n> \n> \n> when we create worker threat itt  has to spawn whole V8 engine , with means it takes lot of time to create the worker threat it self, with in itself happens on a call stack with will block the event loop"},
		{"id":"e2fa972da1a1a216","x":-7840,"y":-1666,"width":185,"height":96,"type":"text","text":"Client requests"},
		{"id":"ee99e3ca6fae6391","x":-7480,"y":-1744,"width":240,"height":252,"type":"text","text":"Call stack\n\nJSON REQUEST BLOCK\n\nREQUEST\nREQUEST\nREQUESTT"},
		{"id":"921b02cdc786df36","x":-6500,"y":-1380,"width":250,"height":60,"type":"text","text":"RabbitMq"},
		{"id":"889480704d86b908","x":-6080,"y":-1580,"width":250,"height":60,"type":"text","text":"Json parser service (different node js maybe)"},
		{"id":"2da93c58de5acdf6","x":-6400,"y":-1860,"width":250,"height":60,"type":"text","text":"Pre warmed worker pool"},
		{"id":"49e95ced0f5f6e6f","x":-3500,"y":-1440,"width":220,"height":240,"type":"text","text":"Task queue\n\n\n1000 request\n1000 request\n1000 request\n\nEvent loop is blocked"},
		{"id":"f6d968c83d1e8263","x":-4360,"y":-1350,"width":250,"height":60,"type":"text","text":"Client"},
		{"id":"1f6148cf4cbd5862","x":-3968,"y":-1532,"width":250,"height":60,"type":"text","text":"Original problem"},
		{"id":"0ef3017af7e5150c","type":"text","text":"Task queue\n\n\n1000 request\n ","x":-2676,"y":-1080,"width":220,"height":240},
		{"id":"87a8ee8939578659","type":"text","text":"Task queue\n\n\n1000 request\n ","x":-2676,"y":-720,"width":220,"height":240},
		{"id":"a002b4dcdf0cdf00","type":"text","text":"Task queue\n\n\n1000 request\n ","x":-2645,"y":-360,"width":220,"height":240},
		{"id":"c2873fc6feb0a971","type":"text","text":"Client","x":-4485,"y":-750,"width":250,"height":60},
		{"id":"3e4ebd2cc88fe3d0","type":"text","text":"Rounde robin load balancer\n\nCluster module or Pm2 clustering","x":-3725,"y":-750,"width":220,"height":200},
		{"id":"e76b4854b0c806d3","x":-3968,"y":-2000,"width":1119,"height":307,"type":"text","text":"**1. Asynchronous Patterns:** \"In a Node.js environment, how do you handle a scenario where a service must process thousands of concurrent database writes without exhausting the connection pool or blocking the Event Loop? Specifically, how would you use TypeScript to ensure type safety across these async operations?\"\n\n\n> \n> \n> \n> \"To handle thousands of concurrent writes, I wouldn't just fire off 1,000 promises at once because I'll hit the PostgreSQL connection limit. I’d use a **Message Queue** (like RabbitMQ) to buffer the load. For the code itself, I’d implement **batching** or use a **concurrency limit** to keep the Event Loop healthy.\n> \n> Regarding TypeScript, I ensure type safety by using **Generics** in my service layers and validating the data coming out of the async calls with **Interfaces** or **Zod**. This ensures that even though the data is moving asynchronously, the rest of the application remains predictable and type-safe.\"\n> \n> \n> \n> "}
	],
	"edges":[
		{"id":"b0d5937b57112d3f","fromNode":"e2fa972da1a1a216","fromSide":"right","toNode":"ee99e3ca6fae6391","toSide":"left","label":"100ms"},
		{"id":"d722b2b1eab45228","fromNode":"ee99e3ca6fae6391","fromSide":"right","toNode":"2da93c58de5acdf6","toSide":"left","label":"delegating big json parser to\npre warmed worker thread "},
		{"id":"6026e23ac5563848","fromNode":"ee99e3ca6fae6391","fromSide":"right","toNode":"921b02cdc786df36","toSide":"left","label":"Or queue to broker and send it to\ndifferent microservice "},
		{"id":"486f0244b8e8cc79","fromNode":"921b02cdc786df36","fromSide":"right","toNode":"889480704d86b908","toSide":"bottom","label":"Delegating it to differente service "},
		{"id":"35780a4fa5116e82","fromNode":"921b02cdc786df36","fromSide":"bottom","toNode":"ee99e3ca6fae6391","toSide":"bottom","color":"3","label":" Optimistic UI to client "},
		{"id":"034f5d4c79afe2df","fromNode":"ee99e3ca6fae6391","fromSide":"bottom","toNode":"e2fa972da1a1a216","toSide":"bottom","color":"3"},
		{"id":"2f77c3ec738947f5","fromNode":"f6d968c83d1e8263","fromSide":"right","toNode":"49e95ced0f5f6e6f","toSide":"left","label":"100000 request per sec"},
		{"id":"8302e2ab9c7e1271","fromNode":"c2873fc6feb0a971","fromSide":"right","toNode":"3e4ebd2cc88fe3d0","toSide":"left","label":"100000 request per sec"},
		{"id":"dfc05d9155eb19ae","fromNode":"3e4ebd2cc88fe3d0","fromSide":"right","toNode":"0ef3017af7e5150c","toSide":"left","label":"Delegating requests to different clusters \nautomaticily re-size the cluster numbers based on load and CPU capbibility\n"},
		{"id":"ef06ad2bb0a33a95","fromNode":"3e4ebd2cc88fe3d0","fromSide":"right","toNode":"87a8ee8939578659","toSide":"left"},
		{"id":"57b4e49cafd09d4a","fromNode":"3e4ebd2cc88fe3d0","fromSide":"right","toNode":"a002b4dcdf0cdf00","toSide":"left"}
	]
}